Course Name: DevOps & Cloud [SWE]
Lecture Title: Kubernetes Storage, HPA & Probes
Start Time: 15/09/2026, 11:00:00
Duration: 120 minutes
Generated by: nase.24bcs10075@sst.scaler.com
Model use: groq, whisper-large-v3-turbo
Downloaded via: Scaler++ Chrome Extension
Developer: Ritesh prajapati

==================================================

Thank you. Thank you. Thank you Thank you. I don't know today it's the day that we went for this traffic I don't know why I thought like we'll start half an hour ago okay What was in the homework? I guess FQDN a core DNS that I told you to revise to resource Section A or B A right or B A Okay Last time session number 10 Yeah We did this rolling update. Correct? And you try this. Okay. And blue green canary and recreate. It was in homework correct done with the homework by the way so I'll share a link today to upload your homework or readmulletmd file you can submit it so this is done this is also done one more thing I give in homework I don't remember what else I give you in homework I guess five things one is a rolling update that's what we did then this deployment second thing anyone noted second one because I don't know what I gave you in one book ah replica set and deployment and then one more thing the same thing uh demon set and i guess deployment and replica set third one okay i remember now yeah and fourth one fourth one what was that yeah so how many of you tried this service type this is another of today's lecture that we are going to cover service how many of you did this cluster IP service type, node port and the service name. How many of you tried Let me know because we have this five demos today and config map and ingress also how many of you tried this no one really everyone went on vacation in the three days really festival days festival mode i can understand okay guys so let's recap the kubernetes okay complete kubernetes what we learned in first lecture master node worker node and its component okay and how many components we have in a master node four components okay which four components etcd scheduler controller fourth one 4.1? API server. Correct. And who is the friend of your cluster? API server. Okay. So can each component can talk with each other directly? No. Through which component they can talk with each one? API server. Okay. And how many components we have in our worker node? Two components. Okay. What are those two components? Qublet and Qproxy and one more thing is there to run our container CRI container runtime interface what we're using by default over there which container runtime we are using container container D container D is the container CRI I mean runtime interface that we are using by default okay Qublet, what it is doing by the way? Qublet? What is the main job of Qublet? Yeah? Communication with the control plane. Two, managed the pod. Create a pod, delete a pod, okay? What is ETCD? key value per data set perfect API scheduler API server API server I guess I should 2 minutes I should have API scheduler Okay What is the main task of API server Main task of API server We need to wake up What is the task of API server? Can you check? Communication, one of the components that we are communicating with each component. component that is one thing we call it as a front door for our cluster that is second thing okay third thing exposes the one of the task okay what about scheduler scheduler's main job it's very easy scheduler new pods okay and And controller? Why we are using controller? We don need controller Why we need controller Cause we need to manage the replicas because we need to manage the deployments correct In deployment we have three replicas and maybe directly replicas that we are creating multiple replicas because we need to manage those replicas and deployment that's why we have a controller. Replica set controller, deployment controller, node controller, lot of controllers are there. Now you've got an idea like till now we created replica sets, daemon set, then the deployment, services. Have you got an idea like how they are connected with each one? At least like controller maybe. Controller you get to know, okay, yeah, we're creating replicas, we're creating deployment. That's why the controller is there. Okay. ETCD you can't see directly, but whatever you are storing it inside an ETCD. That's why whenever you are running the command Qtxt, you'll get nodes. From where the information is coming, that's what the ETCD is. when I'm doing kubectl, get deployment. How it able to know that okay so I need deployment So whatever inside that deployment that we listing the data comes from etc Okay Then API server So whatever we are writing in our YAML file, who is validating those YAML file? After validating your typos, who is telling that there is a typo inside your YAML file? That's what API server is doing. API server is validating your file and creating an API object. everything inside JupyterNet is that we have an API object. Giving you a brief so you can imagine how these components are actually you are working, the replica sets and command. Maybe you already know the commands, right? Right now, you know the rolling update, you know the replica set, you know the service, demon set, at least theory and moreover the commands you know. But you need to understand how it is connected with the components that we are using. Okay? And second thing, few commands I just want to ask, just tell me. To get the nodes which command we are using? kubectl, get nodes. That is one of the instructions that we are using. kubectl get nodes, kubectl get pods, kubectl get svc means service, kubectl get deployments, kubectl get all to get all the things. Okay, anything left? With the get? Yeah replicas, rs. kubectl get rs to get a replica sets. To check the logs, how we can check? logs? kubectl logs and the container, the pod name. Okay. How to delete everything that you have? A deployment service, everything. Delete all, hyphen hyphen, all. Okay. Let's suppose you just want to delete a service then. Delete all service or just kubectl delete service and service name To delete pods same thing to delete replica same thing Okay so you know how to delete how to get the information even the logs Now the main thing how to create those things To create your object, API object, what is the command you are applying? Apply is the command, kubectl, apply, hyphen, f, and the command name. And the second command that we have? Create, okay. So what is the difference between create and apply? Apply can create plus update and create will only create not update. Okay. So what is like if already component is already there, deployment is already created and you are doing the same command again. kubectl create hyphen f and then the deployment dot yaml. Then what will happen? It will create it, it will update it or it will give you an error. If component is already there with the create command error correct And what if I do the apply command instead of create command You will not get error You see nothing will change You can say unchanged. That's it. Okay. Then what we learned so far? Replica set. Replica set and deployment. Okay. Tell me why we need replica set. if we have a pod pod.yaml why we need replica set everything whatever i'm mentioning here it's an interview questions why we need replica set yeah please manage all the pods correct that's the one thing like you can't create 100 pods by just one pod.yaml every time kubectl apply pod1.yaml pod2.yaml you can't do right you just need one file you need to create multiple replicas that's why we need the replica set second thing why we need replicas it's not the only one reason that we need replicas load balancing yes correct second thing is this thought thing the main thing you forgot meaning yeah availability oh the board automatically create scaling it all like scaling is the same Do you know like if I'm creating a pod and if it will go on a crash loop or maybe the pending stage, maybe after running stage completed stage, it will not create another, sorry, it will create another pod, but it is not going to have the same IP address. Every time a pod's internal IP address will change. You want to try, you can try. an Nginx pod, then you make it crash loop something you did the change. Again, you start a new pod and you will see that the same pod that you restarted or maybe the same pod has a crash loop. Now it's working fine, but you will see a new IP address for that particular pod. So every time when a pod will crash and you can say removed and again new pod will come it will have an another IP address okay the IP address will always change for a pod and that is one of the biggest like a risk that we have and that's why we need service even you know why we need service is the only thing it's not like every time we can use the IP address for pod if I'm using the replica set every time I cannot use the IP address of the pod. So that's why we need service. By the service, it will automatically, there is one concept. How many of you resource this core DNS or FQDN? Fully qualified domain name. So these are the two concepts that we have. So replica set automatically resolve the IP address from the name. Okay, the service name. From the service name, let's say service name is a backend. So it will resolve the pod backend pods and it will resolve all the IP address of all the backend pod You got the point That why we need service Okay why we need deployment Manager replica set, one of the important point. Correct. It's not only the migration But yes, for rollback to create strategies there, canary strategies there. Majorly you can say migration is one of the important point because of that we have a different type of deployment strategy even. So that's why we need deployment. But major problem with the replica set, that's why we need deployment. What is that problem with replica set? We can't save the history. Have you ever seen we can save the history in replicas? No replica will create it will delete it will update That it If he wants to save the history then how we can save the history of previous deployments that how we need deployment okay then we have a beautiful demo last time so in today's lecture we are going to see the all type of services so we have a five type of services in kubernetes okay so one is the cluster ip second was the node port third one load balancer fourth one external name or you can mention it as an dns name and fifth one is a headless so it's my content so basically whenever you are searching how many types of kubernetes services that we have over the internet you will find only three cluster ip node port and load balancer okay but if you do the deep research maybe some another documentation or Kubernetes documentation, then you will get to know the fourth service is also there, external name or DNS name. Okay And if you do some more research or maybe after one or two years by using this Kubernetes you will get to know there something like cluster IP none That what we call it as an headless service. So everything I just posted in one chat. So after like learning Kubernetes, you will ask me there's something new we found and then I'll tell you okay. So I did not mention this is fifth type of service. So better I'll add all the five services. Even the most of the devops engineers don't know that we have a five type of services they only know we have a four type of services and some some people only say that we have only three type of services there is no um like external name dns name type of service and headless service but the headless one is one of the advanced um service type that we have and sometimes they don't consider it as in service because there are two types of states and one more thing that you're going to learn in this headless service and for particular that use case we are creating this service I'll tell you how to create it okay so first one that is we have today is a cluster IP. Cluster IP is a by default service that we have. It's an internal service and by this internal service whatever we are deploying the deployment.yaml, service.yaml, replicaset.yaml, whatever we are deploying that we can't able to see in our browser directly until and unless we are doing port forwarding. Port forwarding you did correct. How to do port forwarding that one command kubectl port forward port hyphen forward and the service name and then 808080 that you did correct and then you're able to see the engineering webpage and a first demonstration right so because in that service that we deployed which is using this cluster ip by default now i'll tell you how to use cluster ip okay see this app deployment file So normally, okay this maybe you need to refresh how to write a file. Four things you need to write API version kind metadata and spec Always four things This one we know it a predefined Second thing we are writing a deployment So I'm writing deployment and metadata, the name of the deployment and the label. Why we are putting label here? We can put inside our pod. Why we are putting label here? Do you know why we are putting label here? Any guess? why we are putting label here? Because everything we are mentioning inside the deployment replica set, container in container we are adding the label in replica set we are adding the selectors okay and because it's a deployment.yaml everything we are mentioned here so we need to add a label for our particular deployment so whenever I want to fetch my 10 deployment that I did with this label I can fetch it got it? So replicas 3 means? 3 replicas Correct Selector match labels and then web cluster IP So what it will do My name is what it will do It will match web cluster IP is the selector tag that we need to select. So whatever the pods that created from that you check app and web cluster IP is there and fetch those pods. Got it? In replica set we are mentioning. I told you the difference between labels and selectors so where we are putting labels you know inside of pod.yml so wherever I mentioned the pod I need to put a label so that's why after deploying our thousands of application I can search my pod by particular label okay and where we can search those things so we need to add somewhere now this pod you need to select where we are putting that selector in replica set. Okay. And anything else? Not in just replica set We are putting it inside the deployment tool This is a deployment file Can you see the label Can you see the selector Here also we are putting In service also we can add. Last time in service also we added. Okay, this is a service.yaml. Can you see selector? Now you got to know like in which file we are putting selectors. once in deployment second one second where we're putting replica sets third one service these three files are the common and default file where we are putting selectors and label that we are only putting to the box got it okay the main thing that you need to learn today okay I'm asking you a lot of questions because I think you need to remember everything then and then I guess you will remember at the end of the course okay Kubernetes is ended and Then you need to learn again everything. Okay, what do you mean by ETCD? I don't want that confusion. In every session, at least you need to know what we covered so far in at least Kubernetes. Okay. So can you see? I'm adding Nginx Web as the name of the pod. Then the image that I'm going to use, Nginx 1.25 Alpine. And the pod that we're mentioning, container pod. Again, Christian. What do you mean by container pod? Container port? Port for containers. Okay. Then target port. We explained. Four type of ports I explained. Target port, container port, just pod and one more port. Yeah. Target port for pods port number. Correct. And then just if I'm adding port then Just I am adding port then Service port correct Okay so cause I am adding my container so I am adding my container port here then the resources, so you need to do a best practices for this resource. Okay, again I have a small doubt. What do you mean by 64 mi? Or 128 mi instead of mb? What is the difference between 1 GB and 1 GIB? 1 GB equals to 1000 MB, correct. And 1 GIB means? Correct, correct. Okay. And 50M means? CPU. CPU always we have in a core. Okay. Then 50M means millicore. 50 millicore of CPU. Okay, we are done with this file. Do you know we did not edit anything else something new Last session whatever we did with the rolling update same thing I can see here Nothing will change in cluster IP Okay. So this is my service.yaml. There I mentioned my type of service as in cluster IP. And, okay, something new up here. Okay, let me tell you. So normally whenever we are creating a service.yaml file, we can see API version, kind, metadata and spec. Correct? Four thing default. This one we are adding, kind is as in service, then is service name and label. By default, just blindly put it. Blindly put in metadata, name and labels. By default. Okay? always see whatever you are mentioning here is the same thing in selector is the same thing in label and also check here also control c okay can you see same thing here web cluster IP see the same name also we used here and same name we also used here. Why? It's a basic thing. You guys did few mistakes when I was there so I just noticed and same thing I'm just telling you so you will not do the same mistake. Why the both the labels and selectors are same in deployment and in service? Why? Because we need to connect. How you are connecting your service with your deployment? How you are going to connect your replica sets that you're creating with your deployment, with your service. To connect your service with the deployment you are doing this. If I create a replica set even though I'll do the same thing. Whatever the labels I'm putting inside my service, I'll put the same label inside my deployment.yml or or maybe replicaset.yml, or maybe pod.yml. Whatever I'm creating, service I need to attach with that component. It should be replicaset, it should be deployment, it should be pod.yml, it should be stateful set, it should be deployment, daemon set. Anything can be there, but you need to attach your service with the core component. That's why you are creating service, correct? Okay, a few new things that you need to learn. The first thing, whenever you are mentioning a service, I told you, you need to mention type. What is the type of that service? Maybe it is cluster IP, it's a node port, external DNS, or maybe it is load balancer, or maybe none. Cluster IP as in none. That is our headless. So I mentioned the type of my service. The next thing, selector, that we always mention, and then the port. So usually you need only port and target port Right now you can see something else also Name is https and protocol is TCP This is not necessary to add I just put it by default So it will be easy. So you will learn some new tags also. Otherwise you will see only two tags we need to add. No, you can add more tags. Not even TCP, UDP also you can add. Not only the four tags. There are a lot of tags out there. Whenever it's needed, you can put different tags that you want to use. But the basic thing that we need inside a port is port and target port. Two things are by default and you need to, you make sure you need to update. Okay, this port is for service port. Okay, and target port is for port that you're creating a port. Port has one type of port for that. okay we learn this file is now let me do some hands clear keep ctl okay this one you already did so will not take much time yeah one more thing start your minicube start your minicube I can see a lot of laziness today I don't know by your faces maybe it's a long weekend maybe okay guys first command LS I'll do LS first anyone We will legally say the first command kubectl apply then then app deployment Okay we created second one service same thing and here Service Okay How to check all the component that I created KubeCTL Get all. You see three pods are running. One service is created. Web service cluster IP. And then the replica set is also created. By default. Okay. Now. how do you check your IP address for minikube? minikube? IP. I can see the IP address. As I mentioned, this is my application for Nginx, correct? Nginx application is there and I wants to check my board so I will give you another thing so it's basically a new image new image for you guys so I use the call images call images and this is the version for call image 8.5.0 okay nothing it will do it will just try this command sleep command for 36000 seconds okay so till now your call image container will be up so you can see what is inside your app deployment that you are doing so what I do ctl apply hyphen f and then client and then i'll check again ctl get all where is it where is it yeah this one so now what i'll do i'll just enter inside this container and i'll ping my this service new service okay so how i can do kubectl, how I can exit by the way? How I can enter inside my call client container and I want to ping by this new container? kubectl exit then then hyphen hyphen it then I want to exit inside this container so what i put the same thing okay um something new i wants to do message command what happened type oh go go call client we are wrong command bin bash bin-it it is okay but this is wrong command so okay so okay I switched from kind cluster to minikube so you will also get the same output and this is the consequences and CC good so what will do what will do go here and I'll see there is the same thing right now so we'll just make it up and we'll see Do we have to use hyphen it? No. Something is great, something is great. kubectl exec hyphen it and my pod name kubectl get all so this is my pod I copy this then I'll do kubectl exec hyphen are you guys able to do the same command because I'm getting error it should not be there so this is my pod hmm no directly click on this directly we can enter the command also by call command I have tried a new command what was it? https and cluster ip yes yes i have given the command wait why we are doing it we have the command guys we have the command here i wrote some advanced command advanced command, hyphen hyphen call and the IP address and then the service name. Hmm, yeh wala. Kusho naeha try gatte hain. and then the command and 8080 what is the problem? I should do the same command by the way. I should work in your PC. Maybe today I changed the cluster. Is it the reason or what? no no hyphen hyphen curl and space it's it's just need a space can you see welcome to nginx page but basically i'm able to see this container from my different container so i can i can access my container inside a cluster that is the point that we just need to check so what we did we just created a container we just get inside that container by kubectl exec-it, the pod name, and because we are not able to enter inside a container directly by bin bash or even the sh command was not working So what we did we directly added the command that we wants to do we just need to reach out to that container that we created by hyphen hyphen call hyphen s and this is the service name so it just service name this is the service name that we are pinging and this is the ip address okay and again same command kc kubectl exit and this is the service name and the ip address that we are doing so by pinging this command i'm able to see from another container i'm able to ping my container that is the thing i'm able to see the same thing by the way can you do that can you do right now call is not working now call guys where let me do call command Could not resolve. Same thing. Could not resolve the host So because you call command call command network Any idea guys He has a good question Why the curl command is not working I am able to do the curl command from another container that I created. But I am not able to do from outside of my container. Any idea? That's why we are using cluster IP. Any idea? Why we are not able to do? First of all, how many of you run this command? I will check right now. right now. How many of you run this deployment and service and then another container that you created? Okay. Okay. So can you do the exec command inside the new container, call container, and then inside a call container, you just need to run this service and service IP to reach out to our service. How many of you are able to say welcome to Nginx? Okay. Inside a container that Now, I have a question. I'll tell you the answer, but I have a question. So inside that container if I do crawl command like this I able to get my web service cluster IP Means this is my service and the IP address is 8080 so from another container I able to access my service but if I do the same thing I will copy it okay and then I'll do call I'm not able to do it even though to here I'm not able to see it Why? Anyone have an idea? Because the call command that we are using outside our terminal normally, it is not going to do the call command or exec call command from a container. From a container, we have a particular different internal network. That's why two containers in the same cluster are able to connect. But whatever I'm doing here, call command and the name, it is inside my laptop's network. That means if my website is not, I'm not able to access my website here, then surely cull command will not work here. Do you get the point? So normally. Suppose this is my dashboard, let's suppose scale.com, this is website, okay? So what is this website is about? Scalar.com. So if I do call scalar.com, am I able to get the response or not? Okay, see? At least I'm able to get the page. If I do curl command on this page, I'll get some output. You are being and then something redirected to this website. You see, that means if a website is up and running, then I'll do the curl command inside my terminal. I am able to see some result. But what if I tell you that website is not visible like this, localhost. This is my service. It's not visible, then curl command will not work directly. because whatever you are writing inside your terminal call command will go to your external browser. So even if you are doing call from your terminal or you are directly doing a sourcing inside your external browser both are same thing but what we did here in our service kiftctl get svc so this is our service and what we did we created two containers one is to test just to test this thing we just created a container we did the exact inside that container and then we ping that service is it available or not from another container i'm able to ping that service because both are inside a one container sorry one cluster you got the point both are inside one cluster that's why we are able to ping that service from another container inside the same cluster because this is one of the use case why we are using cluster ip cluster ip it's an internal um service type that we are using inside a Kubernetes to ping the container inside only one cluster. So from the outside, if you want to access your application you don need to use cluster IP Either you need to use the external load balancer of AWS or Azure or maybe you can use your own DNS server or maybe you can use your own external name server also And you can also expose your node port. These are the three ways to get your website outside the cluster. okay what we'll do well we'll have a hands-on first and then for all these services will take five to ten minutes I'll be there and I'll check I'll tell you one of the service that you need to show me engine expect page got it or by one service I will check it will take a lot of time we need to cover a lot of of things today. Okay, be ready with the service. I hope you already deployed this. So whenever I will come, I will say service type. Let's suppose cluster IP, then you need to deploy this and you need to show me your Nginx web page you are able to access from another container and not from the external browser okay second thing kubectl delete all hyphen hyphen all so it deleted now the second one that we are going to learn today okay okay one thing one thing i want from you guys Right now you have template this cluster IP type of service right? But I want to see my website in external browser. How we can do? How we can do? Port forwarding. Okay. Can you do the port forwarding? I'll see. Can you do the port forwarding? I'll delete it. Okay. I'll tell you. Can you do the port forwarding right now? Okay, do the port forwarding and let me know once you are able to see your Nginx default web page. I'll see. You can see. Port forwarding done. Really? I need to check. okay So now we did not mention here. Thank you Let see If you do the AP or the new board you are able to see from which board to which board You are in the deployment of the system. you can get to the bottom right? like the most same you will see and you want to see the port right? if you are using this command you need to see from 8080 to 8080 if you are not that is correct you are not able to see ok? anyone else guys? you have the request you have the request command Thank you Second one Sog Okay guys I have a question You mentioned two boards, 80, 80 and 80. First one is your host board or when we know both? Second one is this? Sog-sup. When we know both of them. yes so Thank you This is a popular movie And who did you play A thousand and a thousand Okay, let's try to do it. Okay, okay. And P Street and APU. Okay, what are you doing? Cluster IP to copy it. Get SVC. Ok guys, one more thing. Get into the Qtctl get SVC. You will see your service name and the cluster IP for that service. If you copy the cluster IP and with the port number that you have, search it inside a browser, you will see the same result. That means service automatically resolve the DNS. Copy the cluster IP by kubectl, get svc-wide, you will see cluster IP, copy the cluster IP, you will see the port after the cluster IP, use that port and then both the cluster Both of this search in your browser. The IP address, colon, and that port number. 8080 or maybe 80. You want to see the same website? so oh Are you able to see the website Okay, maybe in a Mac they are using Docker, then you need to search with new IP. If you are using Mac, there was a little IP for Mac. What was the Qubeman? So, this is the IP address. Yeah. Mac will be called a IP address which will be given Docker. You are not able to see. Yeah. So, Mac, if you are using the service and port number, maybe it will not be seen on the website. Because in a Mac, maybe you install Docker Desktop, right? So Docker Desktop, the IP that you use will change to Docker. So you will see this in external DNS service that we are going to learn today In that you will see the docker desktop will redirect your IP address I will tell you Okay, can we try the second type of service now, node port? Are you done guys, everyone? Done? Can I randomly check one person's laptop? so I'll get assured that you are doing or we'll start with noteport let's see let's start with new one noteport type of service yes so second one map we have noteport so everything is same here same file I just given a new name web server That it Same image I used Okay So app deployment will remain same What I changed the service type I changed Previously it was cluster IP and now it is node port Because it a node port I need to mention my node port because I am not using internal network. I'm not using my internal service network. I need to expose my service outside of my cluster. So I need to give the IP. I need to give the IP or maybe the port number. So I need to expose my node port. Okay. Whenever we are going to learn this load balancer, we need to provide an IP or we need to provide a complete DNS from your Amazon or maybe the load balancer that you are going to use. Okay. So in a node port, we need to use, we need to mention two things. One is the type of the service is equal to noteport and second one the noteport. By default it is 380 by default. If you want to change you can change. Okay. This is our noteport. Nothing is. nothing changed that we did inside our deployment.yaml. So I guess only the tag that we changed. Tag is a web server. Previously it was something else in cluster IP. What was that? Nginx web was there. Okay, and now we just cleared that tag and we updated the tag with web server. Same image we used, same container code, same resources, same, everything is same. We just updated the pod name, web server. Okay, and then service type, what we changed two things type of service noteport and second thing for noteport we need to mention a port noteport and then 380 okay are you guys done let's do some hands-on we deleted everything correct Now we need to do kubectl apply deployment second file service Okay Now we need to check by kubectl get all Yes So my containers are created Noteport container My service is also created. This is my deployment and this is my replica set. Now, I'll see kubectl get svc-o white. Okay. I can see my new service. I can see this IP address. I will copy this IP address with the port 80 okay here on port number 80 It will take some time. Can you do the same thing? Are you able to see the Nginx web page Try with the localhost localhost also work localhost 80808080808080 it not working let me check my node is up or not. it's not up yet. okay kubectl get all this is my node port i copy this node port paste it over here with it i was able to see your nginx default webpage minikube ip okay you can you can try this minikube ip and hit and enter I'll get the minikube ip address I'll copy this I add it and then the port it was 80 default port control set 380 some internet glitches there surely because in ip address and thus that's your notebook you are able to see this is the ultimate thing okay you can do the same thing maybe you are able to see I'm sure this should work. Mac it's not working but window it should. Can you do the same thing? You are able to see in your minikube IP and 380 or maybe the localhost 380. I will do see how many variable to see engineers webpage you will see now like a little issue for math for indeed it's given minicube service only yeah I said I'm in a I don't know for math no, it's CME I have a port in Mac. I have a port forward. I have a port in Docker. I have a port in my history. I don't remember that it is the port. I can try. I don't know which port is Docker. Okay this one I guess 60171 yes this is it for Mac users this is the port 60171 which is the Docker desktop use ok ubuntu no no ubuntu is ok you can use the same thing for mac users if you are using this Docker desktop and minicube then you need to check to check 60171 yes for mac people check it work for you I'm not doing it. What a mess. How do we have to use the tunnel? What do we do? We try it in the new url. Minikube This is going to try something new in the command. Minikube directly. Minikube Services PC service Yes service name Copy Yeah. And then. I've done. I've done. You are. Okay. Okay. Okay, even for Docker, Docker will redirect every time to a new port. This port is. I will try this URL. This is what I want. Because it's working in my EPC. Local host and new port from Docker desktop. This is the port that I got. Okay. You try URL command. Because this URL, it will directly give you the URL. So you can just search this new port number and local host. Instead of localhost you can use your cluster IP. This noteport IP I have to try this and is not working we have to work this okay this trusted ip i have to work this surely this new port token has given us, we will copy it, we will paste it note port ip plus new ip how many of you are able to see the website right now? maybe the Mac also they can use this URL command and other people directly able to see by cluster IP and then the 380 tell me from which approach you are able to see tell me okay directly service IP cluster IP and 380 local hostess check here you have a hyphen hyphen URL Okay, any other people? Any other approach? Yes, you have? It's going to be in local. Any other people? Direct cluster IP. Cluster IP is also possible. Cluster IP, here is the notebook copy cluster IP. Then go to cluster IP and then 380. This is standard. What happened to the server? I have a question now. Bit delay. You get access for servers or? What the problem is that? How do we do this? Okay. Okay, I'll give you 5 minutes. Try this hyphen hyphen URL command. Get the Nginx web page. I'll check right away. check right away. Okay. Till then what we'll do? Okay Thank you Okay guys, tell me how many of you are getting this webpage. I'll just see. Okay, so what else is it? What happened? It went to Nginx platform. Okay, I'll comment. I going to go oh First slide is a direct access to the website. We need to write this over. If you can. Okay, we need to write this over. First slide is a link to write the address and then click on the link. You are able to see your website. Thank you thank you Okay, do I need? Okay, thank you, I'm sorry. Okay, I'll give you a little bit. Okay, I'll give you a little more. Please go. Please do. Please do. Okay. Okay. 300 Shhh Half Shhh 3 3 Cluster Cluster I feel like Source okay sorry um so so external IT generating work water free to get your eyes open no part is not there make you set it take out the report so I'm sorry I'm going to go back to my story Thank you. First class. First class On to those ones How do you mean Let them move this. That means these two are the things. You have no. So, I am here, I will check out... I will check out the case, the case and the case. The case will be I will check out the case. Thank you okay Thank you. So obviously now it's probably just a baby and a baby. okay um So, you can serve it here. Okay. You serve it. So, we need to write it and then we can't find it. Next one. So, we need to write it. Compose it. We need to write it We need to write it We need to write it We need to write it We need to write it Thank you Thank you. Thank you foreign foreign It doesn't go to that. I mean I think it a important Well in history so we can try to make it this first. So, we need to like it to, we need to like it. And we'll have to have something that's happened. We'll have to have a better one. We should start. Okay, so, I think, I will go, I will go. Please go, I will see you in the morning. I will go, I will go. Please go. All in, all in, all in. Another topic I will go. All in, all in, all in. Thank you. and so Thank you. Thank you Thank you. Thank you. Gracias. Gracias. Gracias E a E aí E a please Thank you. thank you um Thank you. Next question is going to be done. Let's go guys. What are we going to do? Cubectl delete. Cubectl delete all hyphen hyphen all. Next alarm try to do it. Ctrl C clear. Yes. And then new one. Okay guys. The third type of service that we have. Which is load balancer. Okay, let's see here. Let's read the load balancer. Okay, can you see same deployment file we used, name is web server. So always whatever the deployment file that we are using, it will be same, just tag will be changed. Okay and service we need to modify because the type of the service is load balancer Okay first one that we did cluster IP which is an internal service type Second one we did which is node port, node port type. And then if you want to see Nginx webpage by the cluster IP you need to do port forwarding. You don't need to do port forwarding in case of node port. That's why we are using node port. Our third type of service that we have which is load balancer. Okay, what do you mean by load balancer? I guess you heard this term in cloud. Load balancer. Balancing the load. Balancing the load. Okay. What do you mean by load balancer, guys? Redirect the traffic among all the pods, among all the microservices, among all the services. Okay. Okay. So do you know that in any cloud platform, whenever we are creating a load balancer, you will get an URL. And from that URL you will just mention the port number and you are able to see your application Normally if I deploying my application in a server then what I do I enable that 80 or maybe 8080 port in security group correct and then I'm able to see my public IP of that server column and then the IP that my service is running. Maybe it's a python then I can see on my IP address of the server and then 5000 My IP address of the server and maybe it's a react then I can see on AT, anything. Okay. That's what we have inside a particular server. But what if I tell you that the same thing that we did using the node port. Okay. And the next thing that we are going to do with the use of load balancer. There is a slightly difference. Only difference is you are using a URL instead of that public IP address. That is the only difference. But, okay, let me try it better. By the way we cannot do it directly in our local host until and unless we do the tunnel command because it is going to create our web load balancer locally in actual environments we have a load balancer big URL we are putting that URL here because it's a local so I just created load balancer locally only the name I provided not any URL you need to to provide them you are here yeah screen share yeah okay yeah yeah yeah yeah As you guys are doing with me, kubectl apply-f app deployment and then solve. service who is it should ctl get call a replica set is there deployment is there and now what i'll do i'll do minicube sbc sbc I don't remember the service name. Somehow, cube ctl get svc. minicube svc service name hyphen o white What is the problem? Minicube has no command in svc, that's why minicube is closed. So right now every time you need to check service okay so you need to run a command minikube svc Because we are using minikube, normally we can do kubectl get svc. But in minikube, we have a command minikube service to get the service info, hyphen o and byte. So, from that service info, I'll get the IP address of that service and the port of that service. So I just need to check in my local browser, IP address and then the port buffer. So I'm able to see my website. Sure. I mean, kubectl apply. To create deployment, I just deleted all the deployment. I need to create it, right? Let's see. redirected and done can you do the same thing command is the same commands are the same Okay Command Command is same Command is same Just type of the service I changed. Okay. Command is same. In minikube we are doing minikube service and service name. And you need to type service. SVC will not work. But in kubectl you can do kubectl get svc You will get the service. See, the service is a very very easy concept. Until and unless you know the difference between each one of the service. So cluster IP is internal node port IP node port URL DNS name is like github Load balancer URL is internal url nahi aata dns mein uska dns name aata hai jase ki github hota hai okay phir load balancer me load balancer ka url aata hai that is the difference like what basic hai you're getting the point how how we are doing this exercises you're getting the point you just need to create deployment you just need to create service and then if you are using minikube minikube service service name that's it okay if if this command is not working hyphen hyphen url if that is also not working do the kubectl get svc and then hyphen o white you ultimately need the ip address and port number to check your website that's why we are running the last command okay how many of you are able to see okay maybe maybe tunnel if you don't have now then there is a command to start a tunnel, minikube tunnel command. It will ask for password. You just need to give your PC password and then you are able to see your website. What it is doing, it's just redirecting your main minikube command. the service IP and the service port to a new service IP in your local host and the port that Docker provided. Okay. Creating a tunnel basically. How many of you are getting this web page? 1, 2, 3, 4, 5. What's the rest of the world? How do we reach the config map? I can't say that you are slow. I will slow you in your mind. So in any laptop if you are not getting this URL, what do you do? First you delete all the deployment. And still if it is not working, then what you need to do? use this tunnel command Once this tunnel command will work and even if it is not working just close this session that you open terminal then again use the tunnel command Once the tunnel command will work all the services issues that you are getting to get your web page it will be resolved Anyone is facing issue, let me know. Let me ask Let me ask You are able to get the engineering web page Yeah, you. Yes, yes. Done? Okay. This side. You are working on the website? Yes, yes. You are working on the website? Yes, yes. Worked on the website? local host how did you get the issue from minikub service you will get the whole IP you are working on any issue? you or you? you are the same issue? you? port? port forward we don't do it I know it's not going to happen. There is another issue but we need to do port forwarding only in cluster IP Why Because it internal and we need to see it That why we are doing port forwarding Until and unless you want to see the website without port forwarding you can use four type of services that we have. Port forwarding is also used in a cluster IP. We don't use it because it is internal IP. Now last demo. External DNS, external DNS. DNS also create. Can you try the last one? External name type of service? Same thing just like deployment service and so on. thing minikube service service name you guys are doing or i'll do their demo and then we'll move forward kya karna hai we'll have a demo or you guys are will do and homework homework me kall hoge three commandes yeah do I will do with you. So you don't think I am doing this. Change directory. 04. Qtl apply-f app development. I can qualify. I fine Hmm it a client pod file QCTL apply hyphen M Client pod file means that you don know what it is What is this? Nothing is, nothing is there. What is it? Call images URL is just a simple container. Call images container. We just put it in the container. 3600 seconds ke liye service.yaml mei meinne daa diya api.gitab.com that is my external dns that i created. qctl apply client qctl apply hyphen f service then ls qctl get all I can see the type as an external name. See can you see one thing If the service type is cluster IP you can see cluster IP If the service type is noteport you are able to see again cluster IP Okay And then even in the load balancer you are able to see cluster IP I mean, the IP address. But in external DNS, you are not able to see the cluster IP, but you are able to see the external IP. Can you see? api.getup.com. This is only one change that I wanted from you. How many of you are getting this external name? Thank you FQDN also we need to cover FQDN, at least FQDN For cluster external name You need to know the FQDN yes guys yes guys it's working external DNS work work on Mac. It's easy. I think it's easy. I don't need to put a code on the code. Just click API.Gitup.com. Just search. That's it. First, what you need to do? You need to apply deployment.yaml, service.yaml, service.yaml i mentioned my own external name api.github.com you can mention anything that you want and then you just need to do apply command for both of this and check it out api.github.com okay how many of you are saying working easy is it not easy 70% of people are telling that it's working I will check. This is a experience Just a little it not going to be a change It's a change. It's a change. Okay. We will change again. We are at Kiana Plain. We are posting the actual link. I want to say and then Yes. Thank you Terima kasih Ini dia sudah ada API yang sudah dikinkan Apakah kita akan melakukan? Same thing dengan Jail Book Dairam PC Post, localhost kemudian dan jahean di website name-nya, code ada So, kita akan melakukan create DNS folder So, kita akan melakukan DNS folder So, kita akan melakukan DNS, kita akan melakukan external name So, kita akan melakukan externalnya Kita akan melakukan PC dan localhost Kita akan melakukan DNS Sampai? Sudho, Memo, PTC, Okay. You can see here, okay, over slash, you can see, over slash, this. I don't want to use, I don't want to mention, but I can't make a business. Is it okay? See you, see you, see you. I don't see it, but I don't want to use it. Okay. Okay. okay See, there's also a fight to check. Because if someone is there, they will be working on their own They will be able to do this They will be able to create a new way So if someone is able to create a new way they will be able to create a local DNS name So we have to create a local DNS name We have to create a DNS name. We have to create a local most. We have to create a local most and then you need to put that name inside the text. inside the text you try to set them Sure, sure, sure. Thank you. forward flash and then forward flash first of all this is coming in first of all this is and then open here and then foreign oh is Okay, guys. Thank you change foreign Oh I Thank you es Gracias. y E aí A gente vai fazer isso e o que muito caro e sim e n lhe deu a sua carta es lo que se puede hacer y y Gracias. Thank you. I'm sorry Server is needed guys Server is needed Now, my house is happening here, the same thing is happening Localhost is not happening in localhost Next lecture, we will have the server to get the next lecture No it didn work with external name or noteport Correct How did this happen This syllabus won complete que se ve Gracias. Gracias. Can you try this command? using different DNS name. Are you able to see any IP address? Do you have a NSLOOKUP? Do you know that your ETC host and then this new one is connected or not? Can you try this NSLOOKUP command inside your pod that you created? So we are just cross-verifying whether your ETC host file and then your service DNS both have synced up or not If you are able to get the IP then check that IP I will try it better it will be better so we have dot okay guys I am doing with you so let's see the email media so that I can not believe I got to know it is and now click password then we are going to localhost then we are going to WQ control x and then it is going to modify yes enter then I will see by cat command yes it's done now I'll do kubectl apply-f service.tml okay then again I will run the same command kubectl DNS is the name and let's look for service this is the service IP I'll search this with the port number up here. It's 53. Hmm. It's not working. L What a problem Something came out of it That means until and unless your DNS is not synced up with your ETC host you are not able to see a website server can't find external database can't find you're getting the same issue already registered dns is on us We can use both the tools and either locally you can sync up them. Locally we don't have any work like that. We need to use a means to use a device. You need to assume guys, you need to assume that your DNS is already there. my own DNS. This is my own DNS. We can try it. If possible, I will get the DNS record for this website. I remember this. We can redirect this. We can add this. But this IP address is also good. Okay, we will do it in the next session once I will get the IP address of my own website because this domain is my domain so that's why I have my IP address and I can redirect that IP address so we can perform this process. Anyone you have your own portfolio on website, deployed website, you can also try this. Now tell us which commands we need to add because we need IP address alias already we have this is my alias and IP address we need to add it inside service and my etc host then we can do I don know the IP address for this okay guys you need to assume for this external DNS Last one, last one. Last one, not so much. Headless service. Stateful set. You need to understand first this stateful set. What does stateful set mean? Okay. I guess I give you some insight regarding demon set. Second is a stateful set. So whatever we are deploying through the deployment all are stateless. Means once you do kubectl get all c see okay so hey me what on silicon routine okay delete all what I'm trying to do to show you this can you do cube CTL get all you will see the pods pod name hyphen something the random name is there Hmm okay See if I do kubectl ls command kubectl apply hyphen f app deployment Okay typo And then if I creating service service.yaml. See, now if I do kubectl, get all. See, can you see this? This is my pod. I can see my pod name. Hyphen something random is there. only difference between stateful set and deployment that stateful set is not stateless stateless just like what we have right now it's a stateless stateless means whenever my first pod will terminate will delete or maybe then crash loop it will create a new pod and then again you will see pod web app load balancer and something new to make that pod unique. Okay. So this is what we call it as in stateless. Every time it is going to create a new pod with a new IP address. But this is inside we have inside. deployment type of one of the component that we have. So if I'm using stateful set that means it will create a particular name that we given. Let's suppose mysql is there. So it will create pod mysql-001 pod mysql-002 and then 003. Now if my first pod that mysql MySQL-001 will be deleted then it will not create the pod MySQL-RandomName. It will create the same name that deleted. So it will create the pod MySQL-001. This is only difference. Got it? What is the difference between deployment and stateful set? set has its own states that it will not create a random name for the pods It will create the name that we define 001002 and basically we are using it for only the backend applications So this is our frontend and backend applications with the help of deployment. But when we need to use the database MySQL Postgres MongoDB at that time we cannot take a risk to use the stateless deployment type. We need to use stateful set. With the use of stateful set you can maintain your DBs and in organization also we are using the stateful set for our Postgres and MongoDB databases. Okay, but for front-end and back-end components you can use this deployment type. It's not only one deployment type, there is another type deployment config. It's an out of syllabus but it's a one of the advanced technique that we are using deployment So here in statefulset we are mentioning statefulset and then we are mentioning the replicas Same thing that we have but whenever it will create that replica you will not see the random names whenever it will delete a pod you will not able to see the random pod will come up it will use the same naming convention that my Kubernetes cluster will have let's suppose I'm using minikube so it will create with the help of minikube it will going to create the three replicas of mysql with a particular name okay try that kubectl delete all hyphen hyphen Can you do the same thing with me? You'll get to know. One more thing, one more thing. Oh, sorry. Sorry I didn tell you Yes See cluster IP should be none This is the one point. So whenever you see cluster IP none, that means that service is headless service. Okay? Headless. Got it? Whenever you see type equals to cluster IP, that means it's a cluster IP type of service. Whenever you see type equals to node port, That means the service is a notepad service. Whenever you see type equals to external name, you will see the name of the service as an external service. But whenever you see cluster IP is none, that means it's a headless service. What do you mean by headless? It's one of the interview questions. What do you mean by headless? So in a headless type of services, we are using kind as a stateful set. What do you mean by stateful set? Means you are not able to create a random names for all the pods or replicas that you are creating. You will see a part particular naming convention for all the ports. Okay. So today's homework, one of the homework that I want to give you, the difference between, or you just do the comparison between stateful set, daemon set, and deployment. Okay. Let's deploy it. kubectl app apply apply hyphen app then service okay and then client pod kubectl get all okay can you see what I told you it is not going to create a random name I using minikube So minikube has its own naming convention hyphen zero hyphen one hyphen two. Let's suppose I'm deleting this one. Let me delete it. kubectl, delete. Pod and then delete it. Now if I will get all, can you see? Same pod is created just two seconds back. That's what I mean by stateful set. You understand what you mean by stateful set and normally the pod that we are creating. Let's suppose I created same application, same thing with the deployment. Then you are able to see random names. Web stateful set hyphen random name should be there. And whenever I deleting that pod it will automatically create again a random name not the same random name but new random name But if I deleting this statefulset 0 it will create the same name That what I mean by statefulset And that what I mean by headless service So whenever we are using headless service we need to mention cluster IP none And it is only specifically we are using for statefulset. You can't tell me that I'll use the cluster IP none in my deployment. No, never. Okay. Okay, I have a question. If cluster IP is none, then am I able to access this service? Outside, I'm not able to access, but even inside? Inside? Cluster IP is an internal IP address. Even though from cluster IP, I'm not able to access my application from outside. But I say like cluster IP is none. That means That means am I able to access this application inside my cluster? Yes or no? Yes what do I say No You are not able to access Because cluster IP is an internal IP if you block the internal IP then we are not able to access it directly Okay until and unless I am the administrator who created that particular node and inside this we created this one the pod Okay then and then Okay last one thing service. I made service.md and clearly mentioned difference. If you ever could have a pod crash. Okay guys, let me explain how to explain what services are in human explanation. Same thing just we discussed in the morning. What do you mean by service, replica set and all things we discussed. So I just briefly 2-3 points which are necessary. why we need service, how we can use service and where we are using service that I mentioned. here okay and what are the problem with the services and that's why we need to use different different type of services that also we mentioned and this is in your homebook to check what are the difference between cluster IP node port load balancer external name and headless you want a brief so I'll give you brief of all the services comparison or this table is enough for you you want a brief tell me I'll give you a brief about all the five type of services that we learned today we had a demo even external name for that we need DNS that I can assure you once I'll get the IP address of some particular DNS we can try that okay guys you need to read this either you read it right away we'll have a two minute or maybe you can read in homework I guess it a time for your I will not take much time Also this cluster IP all the services that we learn where we are using this is the one thing so yeah in interview they will going to ask you that where you are using headless service I told you we are basically using for stateful set type of deployment okay where we are using node port whenever I wants to do the testing in my development environment at that time I'm going to use. Whenever, like when I can use my load balancer type of service. Actual production environment. Because in production we can't expose my IP address. At that time I need some DNS URL. That time I'm using load balancer. And then external name, when I can use external name. When I have my own domain. When I have my own domain, not my client application I have my own product and I want my DNS for that product that time I'm using external name okay when you need to use which type of service that is the main concern here even if you know all the type of services but you don know when you need to use which type of service then it's not okay for you. So you need to learn when you need to use which type of service. When you need to use the headless service, load balancer service, then the node port service, and then the external name service. And for local use cases, we are using cluster IP. okay I put everything important point that you need to learn regarding each service so for every service if you read this document there is nothing left that you need to learn from any external website okay okay so let me cover this small concept FQDN So whenever we are creating one service let suppose backend service I created three pods I created inside my deployment just like we just did right Cluster IP, we created deployment.apply.deployment.yaml. We created three replicas of deployment in GenX. Then we did the apply in service. Then we created service. So both are connected. And then we check one by one service type. What do you mean by FQDM? So, whenever you have a 10,000 of services, then how you're going to resolve the same service name in different, different environment. I have a dev environment, prod environment, test environment. I have a name of the service backend in my dev. I have the name of the service backend in my production. I have the name of the same service backend in my test environment. Then how I'm able to see that I want to access my dev environment backend service. So for that we have this FQDN. With the use of FQDN what you are trying to achieve, you will add your name of the service, then dot and then the environment. Let's suppose dev, let's suppose default, let's suppose production, let's suppose taste that you need to mention here, then dot, it's a service. So svc.cluster.local. If it's a local cluster, you will mention local. If it's another EKS cluster, then EKS cluster has its own DNS, AWS.EKS cluster, something URL is there. So you're going to mention that cluster. So basically your URL will go like PaymentService is the name. So PaymentService.Production.SVC.Cluster.Local. If you are doing with the minikube, you will see the local. If you are doing with some AWS cluster, you have that particular URL for AWS cluster. So for AWS, I remember last time I deployed something, it's like aws.ekse.com something. It's a name so you can see that one. Okay Oh my God guys Sorry guys Okay let me take an attendance Let me tell you. Don't kill anyone. Thank you. Thank you.