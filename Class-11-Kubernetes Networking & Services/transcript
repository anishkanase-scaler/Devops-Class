Course Name: DevOps & Cloud [SWE]
Lecture Title: Kubernetes Networking & Services
Start Time: 08/09/2026, 11:00:00
Duration: 120 minutes
Generated by: ananya.24bcs10019@sst.scaler.com
Model use: groq, whisper-large-v3-turbo
Downloaded via: Scaler++ Chrome Extension
Developer: Ritesh prajapati

==================================================

this is good I mean if someone is taking the session before my session I'm so happy because my mic is working everything is working I don't need to do anything I just start my session even after like seven minutes he took over seven minutes but that's okay it's worth it okay guys have you tried the commands and to keep on it is commands of greatest command these one repo I shared with you if you remember so here we have Kubernetes commands. Have you tried this get commands? Almost all get commands. How many of you tried most of the command I mentioned in this KDIS command What about others This was in your homework I guess Right Okay. Let me try. By the way, first task. Start a minikube. Minikube start. First thing, you start right now. And don't stop it. Okay? So next time when I see the output when I will come then you cannot say to me that my minikube is just starting. Start the minikube. So today we are going to learn pod life cycle, selectors, label, service, deployment and daemon set and how you can scale up scale down replicas. why you need actually ReplicaSet and which point of time ReplicaSet is failing, that's why we need deployment, and how we going to use the deployment with ReplicaSet and all the configuration So first we start with the same application last time we did for Nginx we are going to do the port forwarding and then I want to see the default Nginx webpage in your laptop and then we are going to start with selectors and labels and port lifecycle. Okay? Anyhow we have to complete this otherwise you have extra session. Make sure. Okay, Nginx is running. Qx, Tl, Scc. Service is also running. That means, if I see localhost, Thank you Thank you So first thing that you need to do remove all the pods that are actually running How do you list out the pod? Can you tell me which command? We are listing out the pods, kubectl, get pods. Okay. To list out the namespaces, kubectl get namespaces. To list out the nodes, kubectl get nodes. To list out the replica set, kubectl get replica set or kubectl get rs. Replica set short form rs. To list out the deployment, kubectl get deploy. Only deploy is fine, deployment is also fine. Sometime deploy is working or sometime it's even not working. Let's recap with the commands so you'll get to know then we'll start with other stuff. the cube ctl version cluster info config view it's not needed as of now because uh didn't work with environments namespaces yet right for namespaces you need to use this config get context namespace you want to switch in namespace then you then you are going to use this uh use context also when you are switching between the clusters that also we have only mini cube you're not switching the clusters okay get nodes you know this command get pods svc deploy get all for kubectl get all to see the board services replica set deployment everything that you deployed in your cluster. You can see by kipsutl get all. Okay, events it's kind of a log How to check a log for a particular pod kipsutl logs and the pod name okay you already know i guess okay uh this basic command uh regarding the how how you can check the configuration uh like yaml file what is inside that pod so in docker we have one command docker inspect container and container name docker inspect volume and volume name. Same thing we have a describe command not in spec we have a describe command. So kubectl if we want to describe our pod detail what is inside that pod which image we are using how many resources we are using how many replicas we are using or which container port we are using these information regarding the pod if you want to check then we can run this command kubectl describe pod and pod name. Same for deployment also. Deployment we have application correct. Inside we have a service, inside we have replicas, inside we have a container, container we have them, we have resources and then we have a selectors Okay selectors we are going to learn today Selectors and labels To check the deployment configuration you have a describe deploy command Okay same for the node Okay, if there is a node, there is a server and you need to check all the information regarding that server directly, then you can use this node command. I guess I told you the difference between create and apply hopefully. I told you or not? What is the difference between kubectl apply hyphen f and pod.yml and kubectl create hyphen f and pod.yml? I told you, surely. But let me, yeah? Yeah. Create only, create the file, not update. Okay? And the apply command, if it is already there, the resource is already there, then the resource will be updated will be changed Okay But what is like if you are running a create command the resource is already created and you are running a create command again then you are you will face an error like the resources already existed okay that is the difference between create and apply command same way to delete a pod same thing we were using kubectl delete pod and pod name file also we can delete but we never mostly like 99.99% you are not going to use this kubectl-f file name delete because we never delete the file, we just update the file. Okay, and we do the kubectl apply command. But surely we are going to use this command delete pod, delete node, delete deployment, delete replica set. These are the components. I mean, just like we have a volume and network container and images. Pod is there, node is there, replica set is there, deployment is there, dml set is there, stateful set is there. These are the component and in this component we can do the describe command, get command, delete command, remove command, apply, create, update. These commands are the basic command that we are applying in this Kubernetes core objects. Okay. Okay. Log is one of the command that you know. Exec command. Yeah. So in Docker, how we are performing exec? Docker exec hyphen it and container name. Correct? Same here. We have a kubectl exec hyphen it and pod. We have a pod, not a container. So here, we need to just add a pod name. That's it. Same command. Just kubectl and there we have a docker. today we are going to do this port mapping okay i just updated our devops heroes report devops heroes okay in session number 10 luckily this one yeah we added label and maybe a service yml with the same label or nginx port is there no Label is AppNginx AppNginx is there Yeah AppNginx is there Okay See guys So I tell you one thing So last time we up and run this Nginx right How we did Can anyone tell me How we did this kubectl apply-f pod.yaml so we did this and can you see pod is created how to check the pod kubectl get pods so i can see my pod is up and running okay now i want to list out the services Then kubectl get service or services or svc you can just type. I will do just svc because I mean I don't like to write long long commands. That's why. So as of now I can see my Kubernetes cluster and I can see service type as in cluster IP. There are five type of services. Cluster IP which is default one just like we have a docker bridge network right default bridge network same we have here cluster IP service second service is a node port type of service third service is load manager type of service fourth one is the stateless or you can say headless service headless service is one of the concept and fifth one is a DNS type of service so this five type of service we are going to learn in next session but today we'll learn the basic services cluster IP the default one which we have and second one is a notebook because we wants to see our application in our local host last time a few people like almost like 90% of the people are not able to see the Nginx web page correct because in that your laptop there is a internal network because of that you You need to route it. You need to route your traffic to 8080 or 80 port. So that what we are going to do for this Nginx application But we need service to do that In Docker we don need it right In Docker we just do hyphen p 80 80 and 80 It's enough for Docker. There is only one file, Docker file, correct? But here we have multiple files. Here we have a pod. Pod means a container is running. Right now, I am not directly able to access that pod from my local host. Now I'll tell you how, why I'm not able to do it. then we are going to create a service and then we are going to do a port forwarding same thing you need to do I guess we did it yesterday you Okay, this port you need to know first. Okay, I'll just do it again. See, there are four type of ports that you are going to write in your YAML file. So first port is container port. okay second board is target board thought board is not board and fourth one is just board all four has a different different meaning in your yaml file so this is my yaml file here I can see port, target port and node port and previously last session you run this file and here I can see container port. Okay so let's start with the first one container port for only containers correct that why we call it as a container port so white color yeah okay so container port that we using it is inside the port inside a port and that we have a port for container okay second you have a target port create one node so this is my node number one okay inside I have a board this is my second bar so this is my board one it my board to inside I have a container So I can mention it in container 1 container 2 Okay So when I see container port container port means this port Got my point? This is container port. When I say target port, target port should be this one. Pods port number. So this is 80. For my pod, there is also one type of port. That we call it as a target port. Target port means you can directly imagine, we are not going to deal with this container port because we are not going to create port.yaml usually in big infrastructure. We are going to create a deployment.yaml inside that we are mentioning our target port So container port is like initially when you are learning a Kubernetes it is done and then you are going to use because like for small assessment or maybe like in your portal you have assessment right and let say I will give you some classwork then and then you are going to use this container port most of the time but surely you are going to use the target port. So target port is this one for a port there is a port okay so this one is for I guess I have here container and here we have a pod. Now node port. Node port you have a, there is a node and it has its own code. Server basically, node means nothing else, server, IP address, okay. And server has its code. You creating EC2 instance, you make it as a node, you add it as a cluster. So that means after adding this server inside a cluster it becomes a node. And we know that the server has its own IP address and server has its own inbound and rules, security groups where we can add our ports. So, similarly we have a port for our node. Okay, this is our node port. So, node port we have for a node. Node means nothing but a server. Okay, last one is a just port. This one we have for a service. Now, I will you how we are adding service. Let's suppose there is another node like this. Node 2. Now service 1, s1, service 2, s2, service 3, s3. Here each container is working as in service. Okay each container. Service 1, service 2, service 3. How we are going to manage all this service for that we need service to service communication in microservices For that purpose you need service port This node how it is going to connect with this node and then it will go to this port number then this port number and then you are able to get your actual application. How to access this application. So for this type of architecture or node or node communication, how are two services communicating between each one for that we need a service and for that we need a service port. Okay now it's an advanced concept I just want to give you brief when you have thousands of services each time you are not mentioning the port at the time each time you are not like giving the service URL. So let's suppose like we are using AWS infrastructure so that time for 1000 services you're creating thousands load balancers to just route to that particular service It necessary Okay So there is a solution in Kubernetes they implemented which we call it as an ingress So they created a like you can say worldwide DNS There is an ingress that you are creating from that one URL you are redirecting to thousands of microservices. So let's suppose one load balancer that is costing for one service $20 per month. And if you have a five load balancer for five services, how much it will cost? 100. Right? If you have five services, $100 per month. It's very less amount. Actually, the services are costing around $200 per month for one service. And they are running thousands of service. Can you really like how much like bill we are getting for just redirecting to one service to a load balancer? So to reduce that cost they implemented ingress. ingress is nothing but a DNS URL Okay they implemented the ingress So ingress is working as an like a DNS type of URL And at the end you have two types of routing, path-based routing and the normally default routing that we are doing. Okay, okay, eight minutes. Okay, so that's why they created one universal URL and from here let's suppose my service name is service1 they're giving the service1 URL this URL service2 service2 URL like this thousands of services are working and they are saving around maybe like a pretty much amount by just this in So for these concepts we are actually implementing service. So if you are telling me like why we need to use service we have a first node port here. Okay, we can use this IP, we can use this IP, this IP and then we can go to our port but no it's not going to happen because every time when this service will be down its IP address will change. So how you will get to know that this is the IP address of my one of the pod and I need to restart that pod. How you are get to know? Every time you can't search IP address, right? Because you have a lot of server. So for that purpose, this is one of the pros and cons for our container pod. That's why we need to implement service. And service has its own pros and cons. That's why we need to implement deployment. I tell you. But this is regarding service You get to know there are four types of port mapping we are using One is a container port for container target port for the port level node port for the node level and just port for the service level. Service has its own port number like this okay now how how I can write my service see this is my service.yaml nothing like as always you need to add the four steps epi version kind as in service metadata metadata you can give any name for your service but make sure it should be a standard format. If you are writing a pod, then pod name, hyphen pod. If you are writing a service, service name, hyphen service. Okay, that widget. And then for spec and type node port, okay, this is something new for you. Type means which type of service you are going to use Basically whenever I creating a service I not mentioning it will go the default service IP I tell you how it will go Can you see here kubernetes service is there I just list out the service because I'm running my kubernetes cluster so that's why I got my kubernetes by default service as in cluster IP. Means it's a default one. Now if I'm creating a new service with a node port type of service then I can see a new service with a node port. Just like we have a concept of drivers network right network we have default network same we have a cluster ip as a default one second we created a custom network same way we can create a custom uh like services but we can't create a custom service there is only predefined five type of service that you can implement so this is my service type node port it's predefined already then in a service so whenever we were writing a container, I mean a pod.yaml, this yaml so what we thought like pod means we have a container inside a pod correct So that why we write as an container that means container name container code and container resources so container name we added container image we added container port we added resource we did not edit cause it's like yeah we'll add it in next session because it will be heavy for you directly if I tell you everything standard approach then okay so that's how we are writing a file so whenever you're writing the file you need to make sure what you're writing just like if I'm writing a pod so we know that we have a container inside a board that's why after writing this spec we get to know okay I just need to write a container so that's how you need to think you need to visualize for each one okay same if I'm writing a service then service what what do we have for service Service has its own port, service has its own IP, service has its own URL. So normally we are mentioning the two things. One is a port and one is a type. Service type is mandatory and the second thing is service port. Service port is also mandatory. So service port is 80, target port is 80, target port is for pod level and node port, node port for which type of service you are using. If I'm using a node port type of service then I need to mention node port. This will change with each service type. This line will change with each service type. If I'm adding a node port, it should be a node port. If I'm adding a cluster IP, you don't need to specify. Because cluster is local, it's a default and it's an internal. If I'm adding a load balancer here, let's suppose like load balancer type of service I'm adding load balancer. So this is one of the service. Then I need to mention here load balancer and load balancer complete URL. Okay, that's all. And one more thing You know that how to create a service only two things are required One is a port and one is a type of service Two things is mandatory But now we need to attach this service to our port Correct? Don't you have a question like why we need to attach? Communicate with? Other ports. Other ports, yeah. One correct thing. Why? Why but we need to attach our service with a port? If I'm creating three pods, if I'm scaling three pods, manually kubectl scale, there's a command for scaling. Scaled replica set and then hyphen I would replica three. Now, what if like those scaled pod will crashed? Again, I need to run the same command, right? And I don't have a replica set as of now. I didn't teach you a replica set. Right now, I'm creating this pod by just pod.yaml. So, you are running the pod.yaml for three times. Then you are checking this get pods. Then you are checking the pods. Then you are doing maybe exec command maybe describe command anything Now let suppose those pods are down You need to up it You need to bring up those pods okay at once It's just regarding three services. What if you have thousands of services, sorry, thousands of your pods are there. Then how you're going to manage those things? You know, that's why we need service. Because service doesn't have its own IP address, it has its own port number. Service has its own like a URL. Frontend service URL is there. From frontend service URL, colon and its IP address. So it will go to your frontend port. So inside of frontend, okay let me tell you. It's a very good concept. If you understand the service then you get to know why you need deployment. But you need to understand why you need service. So the concept is for services okay so previously we have a pod like this pod 1 pod 2, pod 3, p1, p2, p3. IP address 10.0.0.1, 10.0.0.2, 10.0.0.3. These type of IP addresses there. When I created my pods from pod.yaml. Okay. Now this pod will crash. Suppose it will crash. Let's suppose there is a life cycle, it goes on a pending state. Let's suppose it goes on a completed stage. Okay, maybe it will go on a crash loop state. These are the stages for a container. Okay, for a pod. Let's suppose these things are happening then really do you actually run a kubectl schedule? Clear up this pod, clear down this pod for particular service if you have thousands of pods are running. Really? Is it a good enough approach? No, right? We can't every time for 10,000 services, we can't do this. So that's why we need something, something like a mechanism who is managing all this thing at once. So this is also front-end pod. Okay. This is also front-end pod. this is also a front-end point. I am doing a management and this is our service. And let's suppose this is an Nginx one. Now you got my point why we need service. Service is the main thing that we are implementing with IP address to scale up our pods to scale down our pods with the help of replicas. Now how we are going to create this 3 pod at once with the help of replica set So now we know that with the help of replica set we created three pods and now how I going to manage those things with the use of service? Okay, now I have thousands of services. Now, how I'm going to manage those services? top of it we have a deployment. And this is the hierarchy. Got my point? Deployment is managing your services plus replica set. Most of the cases, services and replica set. And services actually managing your all the replicas. Got my point? So you can directly scale up, scale down the services by just one command. There is one command. Cube CTL Scale Replica set then replica set name hyphen hyphen replicas equals to 5 So, what it will do? It will scale your replica set. Let's suppose my replica set name is Nginx hyphen RS. so it will scale my replica set whatever replicas are there one two three four ten thousand it will make it five same command we have for our deployment cube CTL scale deployment so there is there is two types of naming convention. I will give it to you both of those names. And then deployment name, deployment name then same hyphen hyphen replicas equals to five so what it will do it will check your deployment and make the nginx replicas to five two types of command one is for replica set one is for deployment you can write it in this manner also this is one of the way so here instead of this one instead of this one you can you can write it like deploy forward slash and the name of deployment Okay and for replica set also you are doing replica set or maybe rs forward slash and are as me. Okay? R is name. One command is a different different variation but the main command will be the same kubectl scale. To scale your deployment to scale your replicaset. You can't scale your pod. Why? Tell me you can't scale your pod. Common sense use karo roli. can scale the pod. Why can't we scale the pod? Because in the pod there is only one container. How can you scale one container? If the pod is only one container, how you can scale it? You can't scale it, right? If there is only one thing inside a big circle, you can't scale it. Until and unless you have three containers, two containers are running, then and then you can scale it right you can scale a pod you scale a replica set you scale a deployment you can scale a pod okay because pod itself we are scaling inside a deployment in replica set what we are doing here We are creating replicas That's what our container, that's what we are scaling right? Now if I'm telling you if you can do the same command for pod, no we can't do. Okay? Now we get to know regarding service okay so this is one type of service node port now I'm telling you these are port yeah here this one it's a default port for our local cluster can you just check like kubectl get svc command are you able to see this port number or any other port number here cluster IP should be there port number is not there, I don't know, hyphen o white. Okay, I'm still not able to see. What you are getting in output? Maybe some mini cube Cube CTL get SVC Cube CTL get SVC miniCube cluster I guess if you using miniCube or maybe something else what you're getting in output same thing explain and everyone is using like miniCube right no one is using kind cluster hopefully how many of you using kind cluster good yes they will give me a like okay this is my error they're not able to resolve it because there is a different different dependencies in different different environments okay so see guys you want to see your website correct nginx one so So you need to make sure that your port is there. You need to export these many ports You need to export this port you need to export this one and then this one and still you need to do a port forwarding from 380 and this one how i write like this yes you need to still do a port forwarding like this these many ports are there i know it's a kind of a mess but once you will understand this port thing once you will get an error then you will get to know so we need to export all this stuff then you are able to see so first one is your service port without your service port you are not able to access inside that service how many containers are running service port you need to do after entering inside the service there is a port Correct? There is a service. Okay? Service port is there. After entering inside that service, you will see, what you will see? Pods. Pods are there. For pod, which term we have? Target port. So for that we need to use the target port. And now I need to see my website outside. outside of my cluster. If you want to see your website outside of your cluster, then you need to export your node port IP. Okay. Basically, you have a cluster IP by default, which is internal, but you want to see your application outside of cluster. Outside of cluster means in local host. So, that means you need to export your cluster IP. So, that's why you need to export your node port. Node port means a server port. Okay. You need to export this one and once you are exporting this one it's a default port for your local cluster once you do this you need to again do a port forwarding from this port to an another port because this port is already reserved that's why you need to do a port forwarding and one more concept how these two things are attached okay so there is a concept for selectors and label Let me explain you So when you have a 10 of services and there is only 50 service of 50 pods are there for Nginx and inside that Nginx pod only 10 pods are there that you wants to fetch then how you're going to manage Kubernetes is all regarding this how you're going to manage because you learn the docker how it is working you just need to manage through the Kubernetes so how you're going to manage if you have a ten thousand service inside that you have a 50 front end services for Nginx specific and you just wants to fetch a specific 10 services you need to add a tag correct you need to add a tag on that particular service when you're creating so let's suppose I'm creating this board so to add a tag we call it as a label just like we have an image tag correct image tag we have so same here we have in kubernetes label we call it as a label So label name and label value This is my label I add it as a labels label name label value Whatever I see here blindly copy it and go inside your service Inside a service you add a selector and paste it Now both are connected Okay Yeah. Here container port. No. No, no, no. Only we need only three ports because we used a node port. we are exporting our service outside of cluster you don't need it if you are exporting outside of cluster then you you are not going to tell that okay this is my service ip i given to you this is my pod ip i given to you i mean the port i given to you but i cannot give my container port because you are exposing your internal port your service port to outside world which is not safe at all. That's why we are not ready. Okay. So second thing is selector. So selector, you can mention as in selector and the same thing whatever you mentioned inside a pod you just need to add it here So whenever you create a pod you create a service and you want to fetch only 10 specific nginx front containers front pods then that you can do by this selector so there is a command kubectl get selector get label from that command you can just list out specific number of pods okay so there is an interview question here so what is the difference between label and selector why you need to use it and where you need to use it and which file you are going to use it you got the point it's a classic question so selector we are using inside a service selector we are not using inside a pod.yaml okay because selector we are using to select specific services so how we can select the specific services inside a service.yaml we can mention a selector App and Nginx, I want to fetch these tag name and the value from all the pods that are running. And inside the pod, you need to mention label. We are giving a tag to that pod. So whenever I want to search that one particular. pod then I can search that particular pod by this label. So label you are using inside a pod.yaml. Why you need to use it? To manage it very well when you are searching a particular service or particular pod inside 10,000 or maybe a large infrastructure is there. Or even if you want to do some changes in particular pod then you need to first search those services correct because it's just a one yaml file at the time you will face around you can say there's a long code base that i'm actually working helm chart so inside the helm chart you will see specific folder for dev stage uat sit production pre-release and inside one each one you will see 26 environment maybe 30 environment in each environment you will see 80 plus services Can you will you it will be around 1 lakh or 2 lakh services And from that services you need to fetch one service you need to update specific file and then you need to do a rollout with zero downtime at that time this tags and labels will help you okay guys now we are going to create the service same way just we did for this one pod.yaml so how How we created pod.yaml kubectl apply-f pod.yaml. Same for service kubectl apply-f service.yaml. How to check the service kubectl get svc. Can you see how nginx service is created? Now one more thing. So after any command hyphen o white. If you are running this command then you are able to see the ip address. suppose for pods we see get pods hyphen 4 white can you see normally for pod I can see only nginx pod one on one running and restart but now if i do the hyphen o white i able to see the pod ip address i able to see this pod is scheduled on which worker node okay that also i can see demo cluster worker node is there inside that this is the ip and this is the board it's a running stage ready state like Anything you want to see the status, even the nominated node. That's an advanced concept. So this information you can see by hyphen o byte command. There is only one command hyphen o byte. At the end of any command, you just try to use this command while you are doing the get command. So you are able to see each detail regarding that particular core component. if it a replica set you can do get rs wipe ok kubectl get rs type was there or what ok there is no replica set we did not create it we create replica set today and I will give you hands on it now we created service and for both things if I look cube CTL get all see get all command will give you every information regarding your board regarding your service right now I created a board which is running then I created an Nginx service which is also running now can you see port number 80 so normally Nginx is listing on port number 80 and then we redirected it to a 380 default port and now I'm going to do a port forwarding. Ok, so this is the port forwarding here. Where is it? We covered it. See kubectl port forward, pod and pod name and the port forwarding command that you want wants to forward. kubectl port forward and the controller why I am writing this? kubectl port controller is there. Yeah, see it's there already. See it's done. kubectl port forward service or SVC also you can write SVC or service forward slash service name and the port that you want to forward. So as of now from my cluster I made it to work as an AT port number. Now from AT port number I need to make sure that it is running on a particular port that I want to forward. Let me check if it's working or not. Close this. This one, this one. Close it. Yeah, it's working. Can you do the same thing now You got my point first thing How the port forwarding is working That is the main concept that you need to understand So from the service see this is my service port when I creating this service I am exporting my node port, so by exporting my node port, from this node, I am redirecting to this port number of service. Okay. And from this 81, I redirected to directly port forwarding to 8080. my point? Anyone? Anyone needs to? Yeah? Done? Okay. Can you do the same thing now? You need to create SVC, you need to create a pod and then you need to do a port forwarding and I will see your Nginx page with port forwarding. Yeah? Do it now. Chalo. Then we need to learn the pod lifecycle and few more files Networking is a little bit tough in Kubernetes So after this session even, you need to learn it by your own, like few things. How many of you are learning after this session Kubernetes? If you are not learning then I am telling you it will be very hard for you. Kubernetes is not an easy concept. Ok. After this session you need to recap the session plus you need to even learn by your own. Ok. There are very few examples I can take. Like very simple example we took but still we exceeded one NAR. Ok guys how many of you are getting the nginx webpage right now Ok I will see right away If you are starting minikubes then you are in danger with people who was just a baby. Thank you. Okay, once we have done this port already, there is a few more files here. you can you can try Thank you Thank you Apoio E aí a ver que me mande a ver o que eu já lhe eu sei que eu vou eu sei que eu vou eu sei que eu vou and just Thank you. I have a flower, this is Rosie over there. In your pen, Rosie. I tried some new commands, so you can get this. We see where the flower man. There are three files recently I just added. I guess there is one, tomorrow, I'm like, is it tomorrow? Tomorrow, tomorrow, yesterday, I had you. Try those files there is a great file and how to do it Go ahead Okay Now you do the controversy and one of the numbers Key. One of the numbers. Three minutes at the moment. Troubleshoot. Yes. So, we tried this troubleshooting when I arrived today. And there is a bit of a file in that. We will get through this command and see how we are getting on. Yeah, troubleshooting. Stand over. Now, I can. Troubleshooting. Troubleshooting. Thank you you can try to use for this just so So there is a creative code of chips inside there I have like the government servers, even set. If we find you just need to run by C++ like hyphen F and Pyneam. Then you run the command, get boards, get nomads, get spc, get replica set, get demon set, all these commands you just need to check. Then next command is C++ described as a particular own component. Okay? Then let me next to our . Okay? So we just go to this now So you see this offline hyperlink even See And if it is fine for London this is fine for the time If you know what is it that is happening it still done with it Yes. You can show. Now, the previous core of GCH will be finding a code in the state of the program. See, you can apply to the iPhone and you can apply it there or apply it. First you apply the deploy mesh service, let's request it. And at last you apply in the stateful set and you apply it. Okay, that's true. So, you can get what's wrong with this. Now, it's a little bit, it's a little bit. See, so much, this one's the move against. y bien y Gracias I will do this I will try another comment in case. I guess after the last session you get it Yeah I did not get it You will Sure we go yeah Okay. foreign It's a stopper. It's a stopper. Stopper is a stopper. It's a stopper. It's a stopper. Open the door. I'm not sure how to play this game. I'm not sure how to play this game. What do you do, Felsen, do you want to know? Thank you. Yeah. This is a good one. It's important to know that. Service is important to know that. Service is important to know that. Instead of the forward service. Yeah. So, we need to use the service. or service forward slash service a specific yourself yeah service forward slash and themselves and then you can take a look at the camera. Now you can take a look at the camera. Now you can see the camera. So, you can take a look at the camera. Thank you Thank you Normally the other way there no other social media The most part of the social media could change the public sector of all communities They can see other communities They can see the people. They can see the people in the community. Normally, five days in this comment have been an idea of a stand, and each move, we are the human power. Just don't think that they're more so that's why they're better to have a lot of time. um foreign Thank you is is I will show you how to do this. I will show you how to do this. I will show you how to do this. Engineering Science Service, use KM. Ye, right? That's the O-port model. That's the O-port model. Okay, so what can I say right now? Okay, you need to learn J-Bal. como que cargan, ¿sabes? Abre el curso. y y Gracias action foreign Thank you but your phone is already open so forward and foreign no Thank you I Okay. . . I used to go in some more than this. um you ready I talk to the front of the room Thank you I before the bridge. If you need to be in there Thank you and then if I really you should use the same one. I am so really used to use the same one. When and how? And why can I keep a new substitute? I have a great day. So I still have a great day. I see what I see. So what does it look like? I do not want to touch. Not to touch. This is the other thing. This match. Thank you and then try the government and try the system on the young and you click on it To secure of that you can get the government and you can get the government and you can get the government check in new commands, explore those new countries just now put the land and then you you and then you Okay, now, with the next step, I will show you some things for you right now. You can try this. But the proper declutter is like a center now. These two are many. Now, you try the declutter. Let's see. Okay. Thank you. yeah Thank you. so I not going to go There is one homework, hard life cycle, running stage, pending stage. I created files to check different types of stages. I guess in your lecture I told you there is one deployment in running stage and one is in completed stage. Hopefully it's your batch, right? Yeah so I created several files inside pod lifecycle And your homework you need to just run this file to check the state of that particular pod It in container creating stage one file if you are running running then you are going to see the running stage then pending.yaml is there if you run that file you will see the pending stage to just give your heads up like how actually this states are working to check those states because some people will tell me like, okay, ma'am, explain this. These many stages are there, but no hands in for the stages. So I created this file. Okay. In your homebook, you just need to run this file and then do the get pods and you are able to see different, different type of container states. Let's say like completed is there. Successful is there. Crash loop is there. Image pull error is there. Completed or maybe running stages there, creating stages there, container create is there. Lot of type of stages are there, you can see. Now the main concept is replica set and the last one is deployment and daemon set We have time Still not working Thank you. How many of you run this replica set in Meta Can you try this Qtl-apply-f and replica set file? I will try the same. It's inside replica set. so change directory replica set then kubectl get svc i need to delete it kubectl station okay first thing that you need to do remove that pod and remove that service okay kubectl delete pod and kubectl delete svc and the name okay two things you need to do i'm doing the same thing if you want to do with me you can do kubectl delete svc and service name ok then kubectl get pods kubectl delete pod engine x-pod it's deleted clear ls we are inside replicaset I have this replicaset.yaml quickly I will explain you what is the difference because now you know how to write a service and you know how to write a pod now it's the next Kubernetes core object which is replicaset replicaset nothing but if you want to create five containers five pods at once and you need to handle those things to handle we are using services and to creating and managing we are using replica set. Replica set is the one of the component with the use of replica set you can create your desired number of replicas maybe you want five you can create a five three two one you can scale it five you can scale it to anything that you wants to do with replicas of pods only replicas of those boards you can manage those things using replica set main work is to manage your replica okay maybe it's create maybe it's update maybe it's delete maybe it's scale down scale up anything related to replica you wants to do you want to perform any operations that's why you need replica set so in replica set kind is replica set and the API version it shouldn't be the v1 it should be the apps and v1 there is an apps and v2 but right now we are going to use the default one apps and v1 okay as you know metadata and then the name So, yatri backend hyphen rs, rs for replicaset, short form. Added a label, okay, just like service, we are adding a label, okay, sorry, just like a pod we are adding a label here Why we are adding a label I tell you Because we did not create a pod We included those pod information in this replica itself That means this replica will create, what is about this replica? It's a Python application. So I give a container name as in backend. I give the image name as in Python Alpine image or 3.11 version. And normally I'm just running a simple command, python-c. That's a basic command to run a python application. So I did not create an app.py. Whatever inside app.py was there, I just paste it over here. That's it, okay? If you don't want to use this long command, you can create your own app.py, you can create your own happy, like normally, hello world one file. But I was so lazy, so I created, I added by one command. Okay, like this. And then the board that I need to mention because it's a container, so I'll mention a port, a container board, 5000. Basically, like Python application is working on 5000 port. by default. Now because I mentioned my container so I need to mention my container port. Okay so this file is similar to pot.yml don't you see? You can see spec container is there and just top of that these thing if I remove the spec replica selector template this thing then it it will be equals to pot.yml correct? If I remove this stuff then it will equals to pot.yaml. So what I'm doing in replica set, replica set you can you can imagine how many replicas you need for a container. So in a pot.yaml now you are adding I want three pot. So in pot.yaml you are mentioning this stuff and you are just changing the kind. That's it. So by just updating a pot.yaml like this you are going to create a replica set just to remember how you can write this file Once you are going to write this file imagine you are writing a pot in pot you are not specifying this stuff only only this thing you are mentioning correct normally spec should be like this but whenever I'm writing my replica set I need to specify how many replicas I need, which image replica I need. So I mentioned first thing replicas. I need three Python replicas for this particular container. Then I need selector. As we know that we are going to attach a service with this application. For service we are adding a selector inside that's service.yml. In replicas we are creating container and in container we are attaching label. That's why we are attaching the label with it. So it is automatically going to attach the label that you provided inside metadata. And the selector also we are providing for replica set. Yes just like service has a selector same way replica set has a selector Both are different different thing So in a service we are doing it to managing all the services to managing all the pods. So here we are creating three pods. We're adding a label to that pod. And if I want to manage those pods by scale up, scale down with the use of ReplicaSet, so ReplicaSet is also using the same concept as in selector. Can you see both name will be same, always. Inside a selector and inside a label. This line will be always same. See, app and Yathri backend. App and Yathri backend. Because we are creating container from this file. We are creating three containers. We are creating three containers for this Python. We are running this command and this is the port. And even we are mentioning the label and even we are adding those selectors. I mean, it will maybe heavy for you to digest, but yeah. If you don want to include this selector and this template this is okay This will work There is no need as of now you can directly just make it indentation correct and then you can run it will create three Python backend replicas but I want to add a selector and labels that's why I'm adding it now how to run it kubectl apply hyphen if What is the name? And then it's done. Now if I want to see get pods You see? Contain is on a creating stage. Again if I do 2 containers are running on and then again if I do my 3 Yacht 3 back end applications are running. Okay? Now how do I check from which one they are running? kubectl get all. If you don't know any command just to kubectl get all, you will get all the clear understanding what you are doing inside a cluster. So let's suppose like I created a 3 rt backend and this is the kubernetes default service and this one we created can you see replica set this is something new previously when you run the command kubectl get rs you're not getting anything because there is no replica set right now if you do the same command you're able to see three replica sets and the same three containers same three pods are running here can you do the same command are you able to see the same thing this is regarding the replica replica is nothing but you're creating a replica set you are mentioning how many pods you need you need to you must add the label you must add the selector and you must define your container that's it only 14 okay Now what if I tell you like you need to attach a service with this and you want to see your backend it running already right so if I do hyphen o hyphen o y okay see IP address and all things I get to know but I don't see any port forwarding right I don't see any 8080 or 80 something I don't see. That means if I do 5000, I'm not able to get that localhost. See, it's not forwarded yet out of my cluster. For that, I need to create a service. And then again, same label I need to add in that service. And then I can see, I can do a port forwarding. You just need to understand how to create replica set. Now I am telling you how you can scale it up, scale down. Three replicas you can see right. Now, kubectl get all right. Now I will scale it up The command I written in my iPad cubectl scale what I need to scale replica set Okay Then RS or maybe replica set forward slash what is the name of that replica set? Yatri backend hyphen RS. This is the name. This one. So I will paste it over here. Then hyphen hyphen replicas. This is the tag equals to 5. Can you see replicas are scaled to 5. Now how I can check kubectl get pods? I am able to see 5 pods. Okay kubectl get pods. You can see 5. Now can you just play around with this command kubectl scale rs or maybe scale replica set forward slash your replica I said name hyphen hyphen replica zero one two three can you just play around you'll get to know how to scale replicas how to scale your containers pods Are you following me How many of you are doing the same thing okay yeah scale down there's no command scale up scale down scale hyphen hyphen replicas and you can mention zero scale up you can mention one you can mention two can anyone try something like cringe thing hyphen replicas minus one Have you tried? What happened? Google people knew how they would be. They would have put a condition that they wouldn't put a minus. I used to do the same thing when I was learning. It's curiosity. Can't kill the curiosity. I guess next command we'll try. How many of you tried this replicas? 5, 10, 1, 2, 0. How many of you tried? Okay, perfect. Now next thing we are going to learn how to get a replica set by the way we just get the cube CTL get pods but do you know how to get the replica set even this is one type of replica set we do cube CTL get RS so So this is only one replica set for backend. So right now, if you are working as a DevOps engineer, you will see thousands of replicas sets. Thousands of replicas set. Okay. You can check by get pods. You will see the containers scaling down. Once you do the replicas hyphen hyphen zero and then enter the command, then hit and enter. command run successfully Okay now you can see the get pods and then you will see five pods maybe after sometime you will do four pods three pods and then it will go into scale down okay sometime it depends on the container or the pod you are using if the service that you are using the application you are using it's a big or heavy service then it will take some time to scale down that's it but it's It's surely scaled up. Okay, one more command. How to delete this replica set? Delete RS and then the replica set. No, no, nothing. Just delete. It will delete it. That's it. If I do kubectl RS, get RS. See, it's done. If I do kubectl, get pods. See, terminating stage. Means if I run again same command, it's in terminating stage. And now it's going to remove these containers. After some time the container will be banished Okay now the main concept the important concept of today one of the concepts. See we know that we have services, we know that we have replica set. Why we need deployment? I'll tell you the deployment.yaml file then you tell me why we need deployment.yaml. This is my deployment.yaml, can you see? So here what I mentioned, I'll just give you a brief. So same as kind, deployment and the metadata and label and selector will be there. Something new we can see here, rolling update, next search, next unavailable. Two to three new fields are there as per the type of deployment. There are four types of deployment. If you have ever given any DevOps interview or crowd interview, then this question will be surely there. Can you explain me the deployment types The first type is rolling update The second one is the canary deployment Third one is blue deployment Have you heard this blue deployment It's a very, very famous name. Okay. So maybe in a company, there is an infrastructure that they are running in Hong Kong servers. Okay. Now you want to migrate from Hong Kong to maybe Singapore servers. So you need to take all the workloads without 0% of time. downtime okay you need to like migrate all the things to Singapore so how you're going to do it without like zero percent downtime at that time what do you need to do there are four type of deployment strategy one is this rolling update you update it in one specific infrastructure you made the all pods down you create a new pods and then you redirect the traffic the basic one ruling update. Second, what is a blue green? So there is one blue and there is one green infrastructure. So green blue infrastructure that you have already it's running. OK, now you are creating same copy of the infrastructure then you are again whatever you are doing here you are doing a new infrastructure you make it up and running and last task is to redirect your traffic flow from your existing one to new one and once the new one is working fine after like one month maybe two months or maybe one week maybe one quarter if your new infrastructure is working fine then you are like scale down all the old replicas are there, old pods are there, deployments are everything inside that your old infrastructure, you make it scale down. This is second strategy. Third strategy that is canary deployment, my favorite one. So that canary deployment what you are doing? So this is your complete infrastructure one and you need to migrate from Hong Kong to Singapore. Now you are taking one part of that infrastructure Let suppose there are seven to eight components in that particular infrastructure seven to eight servers So you put one server and inside that one server you did some changes and then you are testing those changes If it is working fine maybe in one week two week or a quarter everything is working fine then you scale it from one server to two server then again you are testing whether it's working fine again if it is working fine then you are doing it for three servers four server five server 50% 70% it will also go with a linear manner like at starting not linear manner when you're starting you are doing it for 20% of workload then you're doing for 40% of workload then you are doing it for 80% of workload and then again 20% will left and then you are going to completely new infrastructure will be created so that's how canary deployment will work you are testing one component checking testing everything is working fine then you are doing it for like major workloads that's how canary is working and fourth one is there but we are not using it I'll tell you next lecture is a 1pm all day. So what do you think this three strategy I told you which one is more costly Which one is giving the zero downtime what do you think so in a blue green you will get 100 without zero I mean downtime you are able to get a new infrastructure why because you are actually creating a complete new infrastructure then testing it and lastly you are just sending the traffic to old to new replica set but do you think like the one server is costing like that infrastructure is costing you maybe 1 billion and And the same infrastructure you are doing for 1 billion. And then you are scaling down the 1 billion infrastructure. So right now you are creating those things for 2 billion. That is the most costliest part of BlueBrain deployment. And so pros also we have that zero downtime, but there is a cons also that you are costing with your money. Rolling update is easy one. like with zero downtime, you can make those changes and you can do it. And regarding canary, what do you think regarding canary deployment? There is no money lost right Because we are not creating any other infrastructure We just testing on some particular component If it is working then we are moving forward with the other workloads Okay. So in that case, so it depends like which type of infrastructure you have and which client you're working. Based on their requirement, you are doing these strategies. Basically, one Kubernetes administrator will be there and he's deciding which strategy we need to do for migration depending on client need. See in deployment.yaml that we have so this is rolling update and rolling update you have a two particular thing max source and max unavailable. So as you can see three replicas are there. What do you mean by three replicas? Three nginx or maybe three replicas are there and now you are doing rolling update. First thing is why you need deployment if you have already replicas it. You are able to manage all the replicas then why you need deployment to do this migration stuff. Because by replica you can't do migration. I mean from replica how you can do a migration? Tell me. Just replicas you have, you are putting a command in 1000 microservices. How you are going to do that? So to do migration stuff and even let's suppose like in migration you want to change one file, nginx file, but maybe any front end application v1 version is there and you just need to update update it to v2 version every time it will happen if anyone worked on live project related to migration they know that everyone knows from one place to another place whenever we are migrating we are migrating all the tags from v1 to v2 and maybe v2 to v3 v3 to v4 so that's why we get to know this is the new version that we released okay so for that type of issues we are facing in replica set because replica set only we are using for scale up, scale down, create and delete and points Correct But we need some kind of mechanism who is actually managing those replica at C1 And that why deployment is there In deployment we are updating these strategies And in rolling update, this is one type of deployment type. And in that we are adding two parameters, megsearch and mechs unavailable. So megsearch means how many maximum replicas can be there during migration. So if I added three, so maxsourds will do three plus one. So maximum four replicas can be there. There is an old infrastructure, there is a new infrastructure. You are updating something in old infrastructure and then it is going to create new infrastructure. So whenever I'm doing this stuff, so I need to update my files. I need to update my pods level, right? So at that time, it will check the maxsourds. Maxsourds is one. That means three replicas are running. Now a new replica I am creating with a new version. With a new version I am creating a new replica Once that replica is working fine then I I creating another replica But as we know already four replicas are running and max surge is only one That means three plus one is equals to four. Only four pods can run at a time. So at that time, from the three old replica, I need to terminate one pod and then again I need to create another pod with new world. So now I have two new replica sets for new deployment. the new one okay again i can see two old pods are there two new pods are there now four pods are there i cannot create fifth one because my max search is one again it will check the two old replicas will terminate will scale down old replica one container and create a new container with a new image intact that we want in our updated version now we have a three new replica with a new environment that we want now this is what our ask was right This is what our final goal was. We need three new replicas in our new version Now our migration is done So the one replica is pending right now old replica it will terminate and that it That why we need to update So that about the max search. Now the second term is max unavailable. So that we made it as in zero. So maximum how many replicas can be unavailable? Maximum how many replicas can be unavailable? I know this concept is little bit tricky to understand and to even recognize all the stuff, but yes, you need to write it down somewhere and then every day you need to recall it. Maximum unavailable means how many maximum replicas can be unavailable? I said it as in zero. That means three replicas are running and somehow one pod is down and I can see two replicas. It is not needed as of now. So that we are not allowing. Minimum to minimum three replicas should be up and running. Minimum. That's what it is telling. Let's say it's one now. Then, if two replicas are running and max unavailable is 1, is it fine or not? If three replicas are running, my maximum unavailable is 1. 3 minus 1 is equal to 2. So that will work or not? Yes, it will work, surely. So maximum unavailable means how many replicas can be unavailable. 3 we need to minus those unavailability. 3 minus 1 is equal to 2 that means minimum to minimum in my workload 2 replicas should run anyhow minimum to minimum. If I do 0 that means 3 minus 0 3. So minimum 3 replicas should run anyhow. If it is 4 that's okay. If it is 5 that's okay. But minimum I need 3 If I make it 2 then 3 minus 2 1 Minimum 1 replica should run That what How many maximum unavailable replicas is there that you just need to check so max unavailable tell us like two replicas you can neglect from your existing infrastructure okay let me know bit tricky I can explain your game anyone max search Max search means maximum how many replicas you can see while doing migration. So if max search 1 is then 3 plus 1 equal to 4. So maximum 4 replicas should be there. You can't add 5. No. Less than 4 is okay. But maximum after 4 we cannot add. If there is a 5 replica site then max search should be 2. Got it? Got it? No? Okay, we can tell you. What does MaxSource mean? How many replicas we can add extra. I have written here 1. Just a calculation. MaxSource means 1. So, replicas are used to add maxSource. So max unavailable. So max search 1 gave 3 plus 1 is equal to 4. So when I migrate my old one from new one, so 3 replicas are already running. Now I have to migrate from the previous one to new one. So the previous one was 3. Now I have changed one file and created a new pod. So how much will it be? 4 foot run. So max search 1 was 3 plus 1 is equal to 4. That means we can create a replica. Now I have become a new pod. I have 3 pods and 1 is already running. So, there are 4 pods. Now I have to check that I have to migrate new pods. Because there are already 3 pods running. So, I have to migrate to that. So, again, what do I do? I will create a new container. I have a new container. How many containers are there? 5. What is max search? 1. How many can we do? 4. We cannot do it. Correct? So, what do we need to do? The 3 old replicas running from that one, I will down one from that one. then we will create a new port add a new value in that new port and return to the new port Then how many are going to be 2 and 4 How many are going to be 4 That means next search is okay और जितने भी डाउन कर रहे हैं उसमें कभी दो पॉट से नीचे नहीं होना चीए समझें? नाइज कि तीन रप्लिका जो अल्रडी वक कर रही थी और मुझे पुराने वाले से नहीं वाले में मुझे माइगरेट करना है 3-1 इक्वल तू So, मैंने एक रप्लिका को डाउन किया, नही रप्लिका को create किया. चीक है? तो वापसे मैं तीन. So, that's okay. Minimum हमें 3, sorry, 3-1 एक्वल्स एक्वल्स तू. Minimum हमें 2 pods अपन्ट रनी चाहिए. 2 से नीचे pod अपन्ट रनी नहीं होने स्षिए. While you are doing migrating. why we are putting the tag It will never happen You can see the max search limit and the max unavailable There is a tag and it is made for it. No, it can't be made unlimited. Now I have just said three replicas. This is only a small deployment for one service. you have a lot of services, 10,000 services. In 1,000, you are doing so big infrastructure. So, think that there is a 1,000,000 service. In 1,000,000 service, you have two services. So, how many pods will be created? 1,000,000 into 2. How much will it be? In 1,000,000, it will be so much. You said that you have unlimited. If you keep it, you will put the whole world infrastructure. got it so this is the concept sectors so selector and label blindly copy paste after like 4 applications when you are running it will like okay selector, label, you will automatically get out of your hand. Selector and label always, it is a good practice to add and see something new we added version because we are migrating right. We need to add a version 1.0.0. one when I'll do the same thing, I'll update it to 2.000. So I'll get to know that which version I'm migrating from old one to new one. This is your container. Container name is backend. I'm using image python and one command to start that python application. Container port because it's a container and something new again. Resources. This is the tag that we are putting inside a container. So, how much memory I want to give, the CPU I want to give. So, the resources and there is two field, one is a request and one is a limit. I'll explain you. Inside the request, we have a number of resources, and there is you mention CPU and memory and inside the limit you mention CPU and memory. So request whenever you are creating a container you are setting request that bare minimum in my container I need 100 m CPU core, billy core okay and in a memory I need 128 mi memory. Do you know difference between GIB and GB? G I B and G B can you search what is the difference between G I B and G B 24 bits are there in difference one GB means 1000 and the one G I B means 1024 and we got it And this is the difference. So actually we are not using GB here we are using GIV not the MB we are using MI So in request field means bare minimum how much CPU and memory I need And second is a limit field Limit field means if my service is up and running at starting 100 millicord of CPU then service added some features and some workloads are there, traffic is there then it will consume more CPU. After running, it will consume more CPU. running it will consume more more and more user will be there that means you are storing the user data that means memory is also increasing correct so we need to set a limit we need to set a limit that maximum cpu i can allocate to my service after running is 250 millicore if your container is getting CPU 260 millicore, then you will face error like OOO out of memory. Your kernel is killed Then maybe you will face an error like CPU quota exceeded Standard error I telling you For CPU you will get error like CPU quota exceeded and for memory if you don have memory then you will get an error like OO most of the time okay so two things you need to make sure even though you if you don't want to use the limit that's okay you can just directly use the request But for best practices, because in real infrastructure, how we are doing it in real infrastructure, we are specifying both the things. Reason is some legacy applications are there. They are using 1 GB memory, and they are using 25 GB memory, and then something happening inside that specific services, and then they are occupying complete server memory. That means 500 to 600 services are running on particular server, and that one service is occupying every CPU and memory. So what about 499 services that is running inside the service? So this not happened. That why we are putting a limit Actually these things are happening Really recently maybe I don know what time was there some holiday we were we have one holiday a few months back at that time in our open shift cluster do you know open shift kubernetes cluster is there same way a red hat enterprise linux is there right so in red hat they have a one cluster a paid cluster premium paid cluster, open shift cluster. So that cluster we are using in our company. So in that, almost around 700 to 800 bots were there and they just keep down. I don't know what happened. They just keep down. Immediately, I mean, midnight maybe. And then morning, we got to know these things are happening. And morning, all diverse people are like, oh my God. Each people is working on each environment and they are doing up all the services. Scale up will not work at that time. Do you know why scale up will not work? Because 700 pods if a scheduler will schedule, then scheduler will be itself down. Because scheduler is like, it's also one type of server, right? In a one server scheduler is working and so a lot of servers are down 700 to 800 services are down and everyone is just making up 700 services at a time maybe in a five minute ten minute or one hour the scheduler will schedule 800 services scheduler will not work so you need to take a batch of services so batch processing will work batch processing have you heard this concept in computer science. So that time we are doing batch processing. In batch processing we are deploying five services, ten services, twenty services, forty services, fifty services and then one environment is up. So developers will work on that one environment. Then UAT and SIT, those are critical environments. So we'll make it up first. Then we are working on dev environment. So dev environment is usually for testing purposes and they are using. So then we are scale up all those environments. So that's all the strategy we are using. Okay So this is regarding your deployment In deployment you are using the type of strategy rolling update and max source and max unavailable Selectors and labels always will be there Replicas also always will be there. And then regarding your container. Okay. Now, again, same command, kubectl get bots. See, all bots are deleted now. We need to do change directory. deployment which command kubectl apply hyphen f deployment v1 see deployment is created now how to list the deployment just like replica set kubectl get see can you see yatri backend up to date, available is 3. What do you mean by available is 3? 3 replicas See Three replicas are there Now, kubectl get all. You see? Three containers are there and deployment is there. Can you see something new? Replica set by default it is creating. See? So inside a deployment we mentioned replicas even. Here, replicas 3. So in one file, we mentioned container, then we mentioned replicas, we mentioned rolling updates, strategy, even we mentioned resource limit. So everything that we mentioned, we can't mention in directly pod.yaml, we can't mention in service.yaml, we can't mention in replicas.yaml. That's why deployment is one of the important core object in Kubernetes. Even though we can mention service it will be hard for you So next time I will add service at the end so everything whatever we are deploying because we can just mention inside one file one file that it then two concepts will remaining config map and secret to add a secret and environment specific thing we are adding in config map okay okay guys you need to do the same thing run this command deployment command kubectl get apply hyphen f and then the file name. Okay, second file is in your homework, v2 file. You need to run this second deployment. Last concept is remaining, this is demon set. But I told you this daemon set. Let's take some time. You complete this then I'll tell you the daemon set. In your homework you need to run this deployment v2yaml file. Second one pod lifecycle is there. You need to run that even. And then third one, this troubleshooting image. Small exercise for you guys. I added misconfiguration in that file. Once you deploy it you will see the container and you will see some error inside container. Do the exec command and check and then resolve that error. You need to just update something inside your file, maybe one word. Okay, I even I guess two files, yeah. Two files are there. Okay how many of you are getting this deployment? I doing this I not doing this I feel like you not doing this Or you confused nice good learn to carry as an evening car and we look at a lot of me to go after confuse or a chair that a leg right heart to work is over kubernetes are hard of them if you are not practicing okay last concept Now I have a sidecar agent. Do you know FluentDB? FluxDB? FluentDB and FluxDB are like one type of security agent that we are running inside container. check the runtime security, Falco. Same. So I created one agent over here with the use of BusyBox We set the resource event Just one thing is changing here Kind is daemon set. That's it. Daemon set means if you have a 10 server, then each server you want to run one pod. Whenever you're creating server, you need to automatically add that particular pod inside that new node. That's what daemon set is doing. So if you are deploying the daemon set, after deploying the daemon set, how many servers are there? You are adding a new pod in that server. Maybe it's a sidecar container, maybe it's an agent just I created, maybe it's a Farco container to check the security, maybe it's a log collector. This type of container you want in each server. So if you're creating server, once the server is created, automatically daemon set will create one pod inside that server. If you are deleting a server automatically daemonSet will take that particular containers all the data and then delete this over That what demon said okay demon said means you creating a particular sidecar agent or demon inside each node in your cluster inside each node in your cluster same command guys same thing but to do this practical we need cloud we need two to three server. And then if I run this file, then you will see in that three server, automatically one container will be created. Right now we have one local host. Right now we have only one node. So if I'm running it, it will work. But how you can check like it is working in two to three servers. So to do this practical, I guess I will give you a lab session or maybe maybe next session, I'll create my own EC2 instances. And then I'll give you a demo. In one cluster, I'll create three servers. And then I'm going to run this file. Automatically, that three server, it is, going to create this particular pod. Okay? Okay guys, let's start with the attendance. So far what we learned? So far. What we learned today? Can anyone tell me? Thank you.